$('.booking-<%= @booking.id %>').hide();

$('.booking-cancel-<%= @booking.id %>').hide();

var new_confirm = $("<%= j render 'bookings/access', booking: @booking %>")
$('.cancel').append(new_confirm);

