$('.booking-<%= @booking.id %>').remove();

$('.booking-confirm-<%= @booking.id %>').hide();

var new_confirm = $("<%= j render 'bookings/show', booking: @booking %>")
$('.confirm').append(new_confirm);

