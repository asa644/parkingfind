c
impossible_dates
c
    date_ranges.map {|date| date.strftime("%b %d")}
    date_ranges = self.bookings.map { |x| (x.start_at..x.end_at)}
c
reload!
    date_ranges = self.bookings.map { |x| (x.start_at..x.end_at)}
c
    (all_start_dates..all_end_dates).map { |date| date.strftime("%b %d")}
all_end_dates
all_start_dates
p all_start_dates
